import { ClickEventListenerDirective } from './click-event-listener.directive';

describe('ClickEventListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEventListenerDirective();
    expect(directive).toBeTruthy();
  });
});
